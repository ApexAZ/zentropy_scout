/**
 * Task routing management tab (stub).
 *
 * REQ-022 §11.2: Per-provider routing table with task type to model mapping.
 * Full implementation in §17.
 */

/** Routing tab placeholder — implemented in §17. */
export function RoutingTab() {
	return <div data-testid="routing-tab">Routing management coming soon.</div>;
}
