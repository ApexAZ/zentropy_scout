# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=zentropy_scout
DATABASE_USER=zentropy_user
# SECURITY: The default password is for development only.
# In production (ENVIRONMENT=production), you MUST set a secure password.
# The app will refuse to start with the default password in production.
DATABASE_PASSWORD=zentropy_dev_password

# API Configuration
# 0.0.0.0 binds to all network interfaces (required for Docker containers)
# Security: For local development without Docker, use 127.0.0.1 to prevent
# external access. In production, use a reverse proxy with TLS termination.
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration (Security)
# Comma-separated list of allowed origins for cross-origin requests
# Default: http://localhost:3000 (Next.js dev server)
# Production: Set to your actual frontend domain(s)
ALLOWED_ORIGINS=["http://localhost:3000"]

# LLM Configuration
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
GOOGLE_API_KEY=your_google_key_here

# Provider Configuration (REQ-009 §6.2)
LLM_PROVIDER=claude
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536
DEFAULT_MAX_TOKENS=4096
DEFAULT_TEMPERATURE=0.7
LLM_MAX_RETRIES=3

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO

# Authentication (REQ-013 §7.2, §11)
# Local-first mode: Set DEFAULT_USER_ID to your user UUID (no JWT required)
# Hosted mode: Set AUTH_ENABLED=true — JWT cookie required on every request
DEFAULT_USER_ID=
AUTH_ENABLED=false

# SECURITY: AUTH_SECRET is the JWT signing key. In production, you MUST set a
# unique random secret >= 32 characters. The app will refuse to start without it.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET=
AUTH_ISSUER=zentropy-scout
AUTH_COOKIE_NAME=zentropy.session-token
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAMESITE=lax
AUTH_COOKIE_DOMAIN=

# OAuth Providers (REQ-013 §4.1–§4.2)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# Email / Magic Link (REQ-013 §4.4)
EMAIL_FROM=noreply@zentropyscout.com
RESEND_API_KEY=

# Frontend URL (for OAuth redirect back to frontend after callback)
FRONTEND_URL=http://localhost:3000

# Rate Limiting (Security)
# Format: "count/period" (e.g., "10/minute", "100/hour", "1000/day")
# Limits LLM endpoints to prevent abuse and cost explosion
RATE_LIMIT_LLM=10/minute
RATE_LIMIT_EMBEDDINGS=5/minute
RATE_LIMIT_ENABLED=true
