"""Admin Configuration & Management.

Owns read-side lookups (``admin_config_service``) and write-side CRUD
(``admin_management_service``) for models, pricing, routing, funding
packs, system config, and users.  Pure configuration provider with no
domain logic.
"""
